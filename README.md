# Financial Calculator (Static HTML)

A simple, fast financial calculator (loan payment, savings growth, compound interest) built as a static site.

## Run
Open `index.html`.

## Deploy on Render
This repo includes `render.yaml` for Render Blueprint (static site). Create it in Render via **New + → Blueprint**.
